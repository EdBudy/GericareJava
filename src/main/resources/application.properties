spring.application.name=Gericare

# ==============================================================
# ☁️ CONFIGURACIÓN DE BASE DE DATOS (AZURE)
# ==============================================================
# Conectamos al servidor que creaste: gericare.postgres.database.azure.com
# Usamos la base de datos "postgres" que viene por defecto (para asegurar que conecte).
spring.datasource.url=jdbc:postgresql://gericare.postgres.database.azure.com:5432/postgres?sslmode=require
spring.datasource.username=gericare_admin
spring.datasource.password=.Sena2026*
spring.datasource.driver-class-name=org.postgresql.Driver

# ==============================================================
# ⚙️ CONFIGURACIÓN JPA / HIBERNATE
# ==============================================================
# IMPORTANTE: Usamos 'update' en lugar de 'create'.
# 'create' BORRA todo cada vez que reinicias. 'update' mantiene tus datos guardados.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ==============================================================
# 📧 CONFIGURACIÓN DE CORREO (GMAIL) - IGUAL A LA TUYA
# ==============================================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=connectgericare@gmail.com
# Asegúrate de que esta contraseña de aplicación sea válida
spring.mail.password=pqvg qjoq yeio wlau
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.from=connectgericare@gmail.com
spring.mail.properties.mail.debug=true

# ==============================================================
# 🌐 CONFIGURACIÓN WEB / THYMELEAF
# ==============================================================
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
logging.level.org.thymeleaf=TRACE

# OJO AQUÍ: Cuando subas la app (el código) a Azure App Service,
# tendrás que cambiar esto por la URL que te dé Azure (ej: https://gericare.azurewebsites.net)
# Por ahora, si corres desde tu PC conectando a la BD nube, déjalo así.
app.base-url=http://localhost:8080
