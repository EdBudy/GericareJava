spring.application.name=Gericare

# ===============================
# CONFIGURACIÓN DE BASE DE DATOS
# ===============================
spring.datasource.url=jdbc:postgresql://localhost:5432/gericare
spring.datasource.username=postgres
spring.datasource.password=.
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# CONFIGURACIÓN JPA / HIBERNATE
# ===============================
# IMPORTANTE: Usa 'update' para que NO se borren tus usuarios al reiniciar la app.
# Si usas 'create', perderás los usuarios cada vez que hagas un cambio en el código.
spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ===============================
# THYMELEAF (FRONTEND)
# ===============================
# Desactiva caché para ver cambios en HTML sin reiniciar
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
logging.level.org.thymeleaf=TRACE

# ===============================
# CONFIGURACIÓN DE EMAIL (GMAIL)
# ===============================
# En producción (Azure), estas variables se leen desde las Variables de Entorno:
# SPRING_MAIL_HOST, SPRING_MAIL_PORT, SPRING_MAIL_USERNAME, SPRING_MAIL_PASSWORD

#spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
#spring.mail.port=${SPRING_MAIL_PORT:587}
#spring.mail.username=${SPRING_MAIL_USERNAME:connectgericare@gmail.com}
# Contraseña de aplicación: configurar siempre desde variable de entorno SPRING_MAIL_PASSWORD
# (16 caracteres generada en Google > Seguridad > Contraseñas de aplicación)
#spring.mail.password=${SPRING_MAIL_PASSWORD:}

#spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH:true}
#spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE:true}
#spring.mail.from=${SPRING_MAIL_FROM:connectgericare@gmail.com}

# Debug para ver en consola si el correo sale o falla
#spring.mail.properties.mail.debug=true

# ===============================
# CONFIGURACIÓN DE EMAIL (CUENTA SECUNDARIA DE PRUEBA)
# ===============================

# 1. El host y puerto se mantienen iguales para Gmail
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}

# 2. correo secundario
spring.mail.username=${SPRING_MAIL_USERNAME:pedrosanchezalfajor3@gmail.com}

# 3. contraseña de aplicación de 16 caracteres
spring.mail.password=${SPRING_MAIL_PASSWORD:cqvc bkzz kbgp vsfd}

spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE:true}

# 4. Debe coincidir con el username para evitar ser marcado como Spam
spring.mail.from=${SPRING_MAIL_FROM:pedrosanchezalfajor3@gmail.com}

spring.mail.properties.mail.debug=true

# =======================================================
# VARIABLE CRÍTICA PARA EL LINK DE RECUPERACIÓN
# =======================================================
# En producción (Azure), configura la variable de entorno: APP_BASE_URL
# Ejemplo: APP_BASE_URL=https://gericare-web-2026-beh2e0ajecf3h4a4.westus3-01.azurewebsites.net
app.base-url=${APP_BASE_URL:https://gericare-web-2026-beh2e0ajecf3h4a4.westus3-01.azurewebsites.net}
